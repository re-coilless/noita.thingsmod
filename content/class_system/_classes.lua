--name, desc, icon (either a png or a function), items, perks
--skin field can be either png (uses eba's mapping) or a function
--setup_pre
--setup_post
--ability to create subclasses with weighted chances of being selected (works through inheritance)
--lock, hide
--settings

--custom wands (make sure custom items are supported)
--custom skins with in-game painting (cb square, hue and alpha)

--add debug class that only appears if run through dev.exe